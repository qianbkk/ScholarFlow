# Environment
.env
.env.local

# Python
__pycache__/
**/__pycache__/
*.pyc
*.pyo
*.pyd
.Python
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/

# Node
node_modules/
dist/
.npm
.eslintcache

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Test outputs
.pytest_cache/
.coverage
htmlcov/

# Runtime state files
backend/.budget_state.json
backend/.cache/
.run/

# Personal notes & verification artifacts (not for public repo)
# docs/ is intentionally narrow: only ignore internal scratch + audit logs.
# docs/FUTURE_TASKS.md is tracked (project-level roadmap, not personal).
docs/internal/
docs/notes/
docs/audit-logs/
playwright_runs/

# Claude memory: local-only (AI collaboration meta, not for public repo)
.claude/memory/

# ===== R10.5: 开发过程产生的辅助文件不入仓 (本地保留) =====
# docs/ 下的子目录 (notes / audit-logs / internal) 忽略, 但 ARCHITECTURE.md /
# DEPLOYMENT.md / FUTURE_TASKS.md / HANDOFF.md 顶层 .md 是公共文档, 全部入仓
# (上面 L47-51 已逐个列出子目录 ignore, 不需要 blanket docs/)

# tests/ 标准测试套件 (test_*.py + conftest.py) 入仓, CI 直接跑.
# 但 tests/manual/ 是一次性 E2E 验证脚本 (需 Playwright + 真实 LLM key,
# 不是 CI 自动测试), 排除不入仓.
tests/manual/
tests/__pycache__/

# ===== R10.5.18: 收口验证/审计临时文件 =====
# 历史 diff dump (developer 临时导出的 git diff, 用完即弃)
# e2e_screenshots/ + e2e_*.py 是 R10.5 阶段性 bug 复现脚本 (Playwright +
# 真实 LLM, 不可重跑), git history 已经记录了当时状态, 不需要再追踪.
# 比赛材料 (docx/mp4) 本地保留, 体积大且不属于代码.
tests/e2e_screenshots/
tests/e2e_*.py
*.diff
校赛材料/

# ===== 标准开源仓库标配 (入仓) =====
# README.md / LICENSE / VERSION / CHANGELOG.md / CONTRIBUTING.md
# / CODE_OF_CONDUCT.md / SECURITY.md / .env.example 全部保留入仓
# .github/ 仓库协作模板 (issue templates + PR template + CODEOWNERS)
# + CI workflow — 标准仓库必须有, R10.5 重新纳入跟踪
# tests/ + pytest.ini 同步入仓 — CI 跑 'pytest tests/' 必须能定位到
# 实际文件 + 配置 (testpaths + norecursedirs=manual).
